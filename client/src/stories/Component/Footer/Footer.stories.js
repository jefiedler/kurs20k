import React from "react";
import Footer from "./Footer";

export default {
  title: "Components/Footers/Footer",
  component: Footer,
};

const Template = (args) => <Footer {...args} />;

export const MainFooter = Template.bind({});
MainFooter.args = {};
